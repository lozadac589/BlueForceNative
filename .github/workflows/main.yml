name: Build BlueForce APK Cloud Init
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
      
      - name: Force Project Initialization
        run: |
          rm -rf BlueForceNative
          # Usamos el comando más simple y estable
          npx @react-native-community/cli@latest init BlueForceNative --skip-install
          
      - name: Inject Code
        run: |
          # Metemos tu código de Bluetooth
          cp App.js BlueForceNative/App.js
          cd BlueForceNative
          # Instalamos la versión más reciente de la librería de Bluetooth
          npm install react-native-bluetooth-classic@latest --save
          
      - name: Build APK (Debug)
        run: |
          cd BlueForceNative/android
          chmod +x gradlew
          # Ejecutamos la compilación final
          ./gradlew assembleDebug --no-daemon
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: blueforce-apk
          path: BlueForceNative/android/app/build/outputs/apk/debug/app-debug.apk
